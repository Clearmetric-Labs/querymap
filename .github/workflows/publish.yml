name: publish

on:
  push:
    tags:
      - "catalog-core-v*"
      - "query-map-v*"
  workflow_dispatch:
    inputs:
      package:
        description: Package to publish
        required: true
        type: choice
        options:
          - catalog-core
          - query-map

permissions:
  contents: read
  id-token: write

jobs:
  publish:
    runs-on: ubuntu-latest
    environment: pypi

    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Resolve package path
        id: package
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            PACKAGE="${{ inputs.package }}"
          else
            TAG="${GITHUB_REF_NAME}"
            PACKAGE="${TAG%%-v*}"
          fi

          case "$PACKAGE" in
            catalog-core)
              echo "name=catalog-core" >> "$GITHUB_OUTPUT"
              echo "path=packages/catalog-core" >> "$GITHUB_OUTPUT"
              ;;
            query-map)
              echo "name=query-map" >> "$GITHUB_OUTPUT"
              echo "path=packages/query-map" >> "$GITHUB_OUTPUT"
              ;;
            *)
              echo "Unsupported package: $PACKAGE" >&2
              exit 1
              ;;
          esac

      - name: Install release dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install build twine

      - name: Build distribution artifacts
        run: python -m build ${{ steps.package.outputs.path }}

      - name: Validate package metadata
        run: twine check ${{ steps.package.outputs.path }}/dist/*

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: ${{ steps.package.outputs.path }}/dist
